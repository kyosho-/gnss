import { CacheableIntegerArray } from './cacheable-integer-array';

describe('CacheableIntegerArray', () => {
  it('should create an instance', () => {
    // expect(new CacheableIntegerArray()).toBeTruthy();
  });
});
