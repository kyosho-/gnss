import { CacheableSatelliteArray } from './cacheable-satellite-array';

describe('CacheableSatelliteArray', () => {
  it('should create an instance', () => {
    // expect(new CacheableSatelliteArray()).toBeTruthy();
  });
});
