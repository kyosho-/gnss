import { CacheableFloatArray } from './cacheable-float-array';

describe('CacheableFloatArray', () => {
  it('should create an instance', () => {
    // expect(new CacheableFloatArray()).toBeTruthy();
  });
});
